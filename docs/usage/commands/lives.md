# `/watcher lives`

- `roll <TARGETS>`: Rolls `TARGETS`'s (player selector) lives between 2 and 6.
- `set <TARGETS> <LIVES>`: Sets `TARGETS` live counts to `LIVES`.
- `inc <TARGET>`: Increments `TARGET`'s lives by one.
- `dec <TARGET>`: Decrements `TARGET`'s lives by one.
