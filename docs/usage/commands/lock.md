# `/watcher lock`

- `status`: Sends a message to the admin running the command if the session is locked or not.
- `toggle`: Toggles the session lock.
- `add <TARGETS>`: Add currently playing players to the lock's allowed list.
- `adduuid <UUID>` Adds a uuid of any player to the lock's allowed list.
